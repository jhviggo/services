name: Deploying backend
on:
  push:
    paths:
      - 'backend/**'
      - 'infrastructure/**'
      - '.github/workflows/deploy-backend.yaml'

env:
  CONTAINER_REPO: registry.digitalocean.com/viggosdetour-production
  SRC: ./backend
  INFRA: ./infrastructure

jobs:
  push-image:
    name: Build docker image and push to repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Lowercase branch name
        uses: ./.github/actions/env/
        with:
          branch: ${{ github.head_ref || github.ref_name }}

      - name: Build Docker image
        env:
          DOCKER_IMAGE: ${{ env.CONTAINER_REPO }}:${{ env.BRANCH }}-${{ env.GITHUB_SHA_HASH }}
        run: |
          mv ${{ env.INFRA }}/DockerfileBackend ${{ env.SRC }}/Dockerfile
          docker build -t $DOCKER_IMAGE ${{ env.SRC }}
          docker login registry.digitalocean.com -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker push $DOCKER_IMAGE

  k3s-deploy:
    name: Deploy service to K3S
    needs: push-image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Lowercase branch name
        uses: ./.github/actions/env/
        with:
          branch: ${{ github.head_ref || github.ref_name }}

      - uses: azure/setup-kubectl@v3
        id: install

      - name: Configure kubectl
        run: |
          mkdir $HOME/.kube/
          echo "${{ secrets.KUBE_CONFIG }}" >> $HOME/.kube/config
      
      - name: Deploy kubernetes definitions
        working-directory: ${{ env.INFRA }}
        env:
          BRANCH: ${{ env.BRANCH }}
          CONTAINER_REPO: ${{ env.CONTAINER_REPO }}
          GITHUB_SHA: ${{ env.GITHUB_SHA_HASH }}
        run: |
          envsubst < ci-backend.yaml > def.yaml
          kubectl apply -f def.yaml
      
      - name: Wait for service to be ready
        run: |
          kubectl rollout status deployment/${{ env.BRANCH }}-service --timeout=30s
          
      
      
